{
  "name": "@unbound/aggregator",
  "version": "0.1.0",
  "description": "Epoch aggregator — validates receipts, builds Merkle payout trees",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "bin": {
    "unbound-aggregator": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/cli.ts",
    "start": "node dist/cli.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@unbound/core": "0.1.0",
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "@types/ws": "^8.18.0",
    "tsx": "^4.19.3"
  }
}